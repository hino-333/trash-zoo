# Trash zoo — 作業する人（人間・エージェント問わず）へ

その日出した食べ物のゴミを1つ放す。放した人だけが、他の人のゴミを見られる。
72時間で工場に運ばれ、二度と見られない。

これは SNS ではない。**編集できない他人の生活の証拠**だけを流通させることで、
「自分以外は上手くやっている」という歪んだ比較の分母を静かに補正する。
説教はしない。環境問題も語らない。ユーザーには「他の国の人が何を食べているか見られる場所」としてだけ体験される。
効果は構造が担う。メッセージは表に出さない。

## 迷ったらこの5つで判断する

1. **発信者が編集できない** — 撮影対象・見せ方の裁量をユーザーから外す。切り抜きも説明文も自動。自由記述欄は作らない
2. **誰が見るかわからない** — 見られる相手が想像できた瞬間に人は盛る。投稿先の地域は選ばせない
3. **投稿の動機が「見せたい」ではない** — 投稿は入場券であり対価。承認を求める行為にしない
4. **見る側に評価手段がない** — 評価があると発信側が最適化を始める
5. **平凡なものほど価値がある** — 特別な日を投稿したくなる構造を作らない

## 実装してはいけないもの

いいね / コメント / 返信 / フォロー / DM / ユーザー名 / プロフィール / アイコン / ランキング /
「人気の放飼場」/ 投稿数の個人表示 / SNS共有ボタン / 連続ログインボーナス / デイリーミッション / 通知の乱発。

数字を出すなら「増やす努力ができない数字」だけにする（来園者数、本日の匹数、出荷数はよい。個人の投稿数は駄目）。
文言に感嘆符を使わない。煽らない。急かさない。施設として話し、「私が」とは言わない。

## ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | 画面の器。**全ての script/link に `?v=N` が付いている**（後述） |
| `style.css` | 色、影、跳ね方、来園者のポーズ |
| `app.js` | 島の生成、放飼場、個体、入場券、投稿フロー、切り抜き、工場 |
| `scene.js` | 放飼場の中の風景（空・遠景・擁壁・地面・ガラス柵・来園者） |
| `data.js` | 地域定義、タイムゾーンからの地域判定、初期個体 |
| `api/judge.js` | Vercel の関数。Claude vision で判定し飼育員の記録を返す |
| `judge-prompt.md` | 飼育員のプロンプト（`api/judge.js` の中身と同じもの） |

ビルドは無い。素の HTML/CSS/JS を静的配信しているだけ。`npm install` はサーバー関数のためだけに存在する。

## 配信

- 本番: <https://trash-zoo.vercel.app/> — `main` に push すると自動デプロイ（30〜60秒）
- ミラー: <https://hino-333.github.io/trash-zoo/> — GitHub Pages。静的のみで `/api/judge` が無く、判定は暫定の飼育員に落ちる。動作確認は必ず Vercel 側で
- 判定には Vercel の環境変数 `ANTHROPIC_API_KEY` が要る。**キーはサーバーにだけ置く。クライアントに置かない**

ローカルで見るとき:

```bash
npx --yes http-server -p 5601 -c-1 .
```

判定の疎通確認:

```bash
curl -X POST https://trash-zoo.vercel.app/api/judge \
  -H "Content-Type: application/json" \
  -d '{"image":"data:image/png;base64,..."}'
```

## 踏んだ落とし穴（同じ穴に落ちないこと）

- **`?v=N` を必ず上げる。** CSS/JS を編集したら `index.html` の `?v=` を全部インクリメントして push する（`sed -i '' 's/?v=10/?v=11/g' index.html`）。忘れると古いキャッシュを掴んだまま「直っていない」ように見える
- **CSS アニメーションは SVG の `transform` 属性を上書きする。** 位置を `transform="translate(...)"` で指定した要素に、`transform` を動かす CSS アニメーションを当てると位置が飛ぶ。位置用の `g` と、揺れ用の `g` を必ず分ける
- **`#pen` の寸法は overlay を表示してからでないと測れない。** `openPen()` は先に `$('enclosure').hidden = false` してから `clientWidth/Height` を読む
- **localStorage はオリジンごとに別。** Vercel 版と Pages 版で入場券も個体も別管理になる
- **切り抜きの領域拡張は「隣どうしの比較」で書くと輪郭のぼけを伝って対象まで食う。** いまは縁の平均色を基準にしている（`app.js` の `cutout()`）。ここを触るときは必ず実写で確認する

## 残っている作業

1. **切り抜きの品質** — いまはブラウザ内の自前処理。背景と対象の色が近いと残る。`@imgly/background-removal` などと比較して差し替える。**ここが甘いと部屋が写り、盛る余地が生まれる。このプロダクトの生命線**
2. **保存** — いまは端末の localStorage のみ。他人のゴミは `data.js` の初期個体で代用している。サーバーに移す際も、保存するのは切り抜き後の画像だけ。元画像は持たない
3. **72時間の工場行き** — いまはクライアント側で判定している。サーバーに移したら削除ジョブが要る
4. **匿名ID** — 端末単位の匿名ID。メールもパスワードも持たない。個人を特定する情報を一切持たない

## 品質の判断基準

- 背景が静止していないか。動いていなければ死んでいる
- 影のないオブジェクトはないか。光源は左上で統一されているか
- ゴミの跳ね方は全部同じになっていないか（`weight_class` で描き分ける）
- どこかに「盛れる余地」を作っていないか
- 評価に繋がるものを追加していないか
- 文言が煽っていないか
